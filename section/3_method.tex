本研究では第2章で述べた方針に基づき、以下の要素を組み合わせた重み付けを提案する。

\section{自然言語処理モデルを利用した距離計算}
求人タイトルや業務内容、職務経歴などのテキストを自然言語処理モデルでベクトル化し、文書間の距離（類似度）を計算する。これにより職種的距離やスキル的距離を定量化できる。

\subsection{ベクトル化とコサイン類似度}
自然言語処理モデル（BERT等）を用いて、テキストを高次元ベクトル空間に埋め込む。具体的には、求人タイトルや業務内容などの各テキストフィールドをモデルに入力し、固定長のベクトル表現を得る。

テキスト間の類似度評価には、ベクトル化された表現間のコサイン類似度を用いる。2つのベクトル\(\mathbf{a}\)と\(\mathbf{b}\)のコサイン類似度は以下で定義される：

\[
\cos(\theta)=\frac{\mathbf{a}\cdot\mathbf{b}}{\|\mathbf{a}\|\,\|\mathbf{b}\|}=\frac{\sum_{i=1}^{n} a_i b_i}{\sqrt{\sum_{i=1}^{n} a_i^2}\,\sqrt{\sum_{i=1}^{n} b_i^2}}
\]

ここで、\(\mathbf{a}=(a_1, a_2, \ldots, a_n)\)および\(\mathbf{b}=(b_1, b_2, \ldots, b_n)\)は\(n\)次元ベクトル、\(\mathbf{a}\cdot\mathbf{b}\)は内積、\(\|\mathbf{a}\|\)および\(\|\mathbf{b}\|\)はそれぞれのベクトルのユークリッドノルムを表す。

コサイン類似度は\([-1, 1]\)の範囲の値を取り、1に近いほど類似度が高く、0は直交（無関係）、-1は完全に反対の方向を示す。テキスト解析では通常、非負の値域\([0, 1]\)で扱われる。

\subsection{行列間の類似度計算}
複数のテキストフィールド（例：求人タイトル、業務内容、介護項目など）を持つ文書全体の類似度を計算する場合、各フィールドをベクトル化した後、それらを行列として扱う。

文書Aのフィールドベクトルを\(\mathbf{a}_1, \mathbf{a}_2, \ldots, \mathbf{a}_m\)、文書Bのフィールドベクトルを\(\mathbf{b}_1, \mathbf{b}_2, \ldots, \mathbf{b}_m\)とする。このとき、行列間の類似度は以下の手順で計算される：

\begin{enumerate}
\item 各フィールドペア\((\mathbf{a}_i, \mathbf{b}_i)\)のコサイン類似度\(s_i\)を計算
\item 各フィールドに重み\(w_i\)を付与し、加重平均を求める：
\[
\text{Similarity}(A, B) = \frac{\sum_{i=1}^{m} w_i \cdot s_i}{\sum_{i=1}^{m} w_i}
\]
\end{enumerate}

この手法により、複数の観点から文書間の類似性を総合的に評価できる。重み\(w_i\)は実験的に調整され、本研究では7つの項目（求人タイトル、業務内容、介護項目、施設、勤務時間、時給、勤務日）に対して様々な重み付けパターンを検証した。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{images/fig3-1.pdf}
\caption{求人推薦スコア算出の全体フロー}
\label{fig:score_overall_flow}
\end{figure}

\section{過去応募履歴の活用}
本研究では、ワーカーの過去の応募・勤務履歴を用いて嗜好ベクトルを構成し、次に選ばれる求人を推定する。実験では「学習用に用いる仕事履歴」と「評価に用いるテスト仕事履歴」に分け、後者を固定し、学習件数を段階的に変化させた結果を評価した。

\subsection{学習・評価プロトコル}
各ワーカーについて、履歴の新しい順から固定テストデータを10件確保し、その後に続く履歴データから$k$件を確保する。そこから算出した類似度より固定テストデータ10件に対してTop-K精度を算出する。$k$は段階的学習の学習ステップ（例：5,10,15,20,\ldots）として変化させ、学習件数が増えたときの性能変化（学習曲線）を比較した。

\subsection{ランキング生成（候補求人のスコアリング）}
学習区間に含まれる各求人を自然言語処理モデルでベクトル化し、履歴ベクトルの平均（必要に応じて時間重み・項目重みを適用）を嗜好ベクトルとする。テスト区間の各セッションに含まれる候補求人も同様にベクトル化し、嗜好ベクトルとのコサイン類似度によりランキングを作成する。

\subsection{重み付けと拡張}
第4章の実験2では、履歴と求人を構成する複数項目（職種名、業務内容、介護項目、施設、勤務時間、時給、勤務日）に対して重みを付与し、項目別類似度の加重平均として総合スコアを算出した。また、履歴の新しさを反映するため、履歴求人に対して時間重み（指数減衰やウィンドウ等）を適用し、最近の行動をより強く反映する設定を比較した。さらに、回避傾向のある業務を含む求人にはペナルティを付与し、ランキングから外れやすくする拡張も評価した。
